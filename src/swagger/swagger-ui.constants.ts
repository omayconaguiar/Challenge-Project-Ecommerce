import { SwaggerUIOptions } from './swagger-ui-constants.interface';

export const DEFAULT_SWAGGER_UI_OPTIONS: SwaggerUIOptions = {
  customSiteTitle: 'My API Docs',
  customCss: '.swagger-ui .topbar { display: none }',
};
